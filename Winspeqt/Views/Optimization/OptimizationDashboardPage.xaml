<Page
    x:Class="Winspeqt.Views.Optimization.OptimizationDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <Grid Padding="60">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with Back Button -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button 
                    Grid.Column="0"
                    Content="← Back to Dashboard"
                    Click="BackButton_Click"
                    Margin="0,0,20,0"
                    Background="Transparent"
                    BorderThickness="0"
                    FontSize="16"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock 
                        Text="System Optimization &amp; Cleanup" 
                        FontSize="36" 
                        FontWeight="Bold"/>
                    <TextBlock 
                        Text="Free up space and improve performance safely" 
                        FontSize="18" 
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Margin="0,8,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Feature Cards Grid -->
            <Grid Grid.Row="2" Margin="0,40,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" MinHeight="280"/>
                    <RowDefinition Height="*" MinHeight="280"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Large File Finder Card -->
                <Button 
                    Grid.Row="0" 
                    Grid.Column="0"
                    Margin="0,0,16,16"
                    Padding="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2"
                    CornerRadius="12"
                    Click="LargeFileFinderCard_Click">
                    <Grid Padding="36">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <FontIcon 
                            Grid.Row="0"
                            Glyph="&#xE8B7;" 
                            FontSize="56"
                            Foreground="#F44336"
                            Margin="0,0,0,20"/>

                        <StackPanel Grid.Row="1" VerticalAlignment="Center">
                            <TextBlock 
                                Text="Large File Finder" 
                                FontSize="24" 
                                FontWeight="SemiBold"
                                Margin="0,0,0,12"/>
                            <TextBlock 
                                Text="Scan your folders to find the largest files taking up space on your PC"
                                FontSize="15"
                                TextWrapping="Wrap"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                LineHeight="22"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE76C;" FontSize="14" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock 
                                Text="Find Large Files" 
                                FontSize="15"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                Margin="8,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Button>

                <!-- App Usage Tracker Card -->
                <Button 
                    Grid.Row="0" 
                    Grid.Column="1"
                    Margin="16,0,0,16"
                    Padding="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2"
                    CornerRadius="12"
                    Click="AppUsageTrackerCard_Click">
                    <Grid Padding="36">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <FontIcon 
                            Grid.Row="0"
                            Glyph="&#xE74C;" 
                            FontSize="56"
                            Foreground="#2196F3"
                            Margin="0,0,0,20"/>

                        <StackPanel Grid.Row="1" VerticalAlignment="Center">
                            <TextBlock 
                                Text="App Usage Tracker" 
                                FontSize="24" 
                                FontWeight="SemiBold"
                                Margin="0,0,0,12"/>
                            <TextBlock 
                                Text="See all installed apps with last used dates, sizes, and uninstall suggestions"
                                FontSize="15"
                                TextWrapping="Wrap"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                LineHeight="22"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE76C;" FontSize="14" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock 
                                Text="View Apps" 
                                FontSize="15"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                Margin="8,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Button>

                <!-- AppData Cleanup Card -->
                <Button 
                    Grid.Row="1" 
                    Grid.Column="0"
                    Margin="0,16,16,0"
                    Padding="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2"
                    CornerRadius="12"
                    Click="AppDataCleanupCard_Click">
                    <Grid Padding="36">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <FontIcon 
                            Grid.Row="0"
                            Glyph="&#xE74D;" 
                            FontSize="56"
                            Foreground="#FF9800"
                            Margin="0,0,0,20"/>

                        <StackPanel Grid.Row="1" VerticalAlignment="Center">
                            <TextBlock 
                                Text="Safe AppData Cleanup" 
                                FontSize="24" 
                                FontWeight="SemiBold"
                                Margin="0,0,0,12"/>
                            <TextBlock 
                                Text="Identify and remove outdated cache, temp files, and unneeded data safely"
                                FontSize="15"
                                TextWrapping="Wrap"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                LineHeight="22"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE76C;" FontSize="14" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock 
                                Text="Start Cleanup" 
                                FontSize="15"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                Margin="8,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Button>

                <!-- One-Click Optimization Card -->
                <Button 
                    Grid.Row="1" 
                    Grid.Column="1"
                    Margin="16,16,0,0"
                    Padding="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2"
                    CornerRadius="12"
                    Click="OneClickOptimizationCard_Click">
                    <Grid Padding="36">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <FontIcon 
                            Grid.Row="0"
                            Glyph="&#xE73C;" 
                            FontSize="56"
                            Foreground="#4CAF50"
                            Margin="0,0,0,20"/>

                        <StackPanel Grid.Row="1" VerticalAlignment="Center">
                            <TextBlock 
                                Text="One-Click Optimization" 
                                FontSize="24" 
                                FontWeight="SemiBold"
                                Margin="0,0,0,12"/>
                            <TextBlock 
                                Text="Run a complete cleanup: temp files, recycle bin, logs, and more in one click"
                                FontSize="15"
                                TextWrapping="Wrap"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                LineHeight="22"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE76C;" FontSize="14" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock 
                                Text="Optimize Now" 
                                FontSize="15"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                Margin="8,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Button>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>