<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Winspeqt.Views.Optimization.LargeFileFinder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winspeqt.Views.Optimization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:Winspeqt.Helpers" 
    xmlns:optimization="using:Winspeqt.ViewModels.Optimization" d:DataContext="{d:DesignInstance Type=optimization:LargeFileFinderViewModel}"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="60">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with Back Button -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button 
                 Grid.Column="0"
                 Content="← Back"
                 Click="BackButton_Click"
                 Margin="0,0,20,0"
                 Background="Transparent"
                 BorderThickness="0"
                 FontSize="16"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                        <FontIcon 
                            Grid.Row="0"
                            Glyph="&#xE8B7;" 
                            FontSize="56"
                            Foreground="#F44336"
                            Margin="0,0,10,10"/>
                        <TextBlock 
                             Text="Large File Finder" 
                             FontSize="36" 
                             FontWeight="Bold"/>
                    </StackPanel>
                    
                    <TextBlock 
                     Text="Scan your folders to find the largest files taking up space on your PC" 
                     FontSize="18" 
                     Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                     Margin="0,8,0,0"/>
                </StackPanel>
            </Grid>
            <ListView Grid.Row="1" ItemsSource="{Binding FolderItems, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <FontIcon Glyph="&#xE76E;" Margin="0,0,4,0"/>
                            <TextBlock Text="{Binding }"
                           FontSize="24" Foreground="Green"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.Items>
                    
                </ListView.Items>
            </ListView>
            <!--<Button Grid.Row="2" Content="Test" Click="" />-->
        </Grid>
    </ScrollViewer>
</Page>
