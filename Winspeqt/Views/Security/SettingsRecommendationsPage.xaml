<Page
    x:Class="Winspeqt.Views.Security.SettingsRecommendationsPage"
    x:Name="TaskManagerPageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:Winspeqt.Helpers"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextWrapping" Value="WrapWholeWords" />
        </Style>
        <Style x:Key="SectionIconStyle" TargetType="FontIcon">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,2,8,0"/>
        </Style>
        <Style x:Key="SectionBodyText" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="WrapWholeWords" />
            <Setter Property="Margin" Value="0,0,0,20" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="WrapWholeWords" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </Page.Resources>

    <ScrollViewer>
        <Grid Padding="60">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button 
                    Grid.Column="0"
                    Content="← Back"
                    Click="BackButton_Click"
                    Background="Transparent"
                    BorderThickness="0"
                    FontSize="16"
                    Margin="0,0,20,0"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock 
                        Text="Windows Settings Recommendations" 
                        FontSize="36" 
                        FontWeight="Bold"
                        TextWrapping="WrapWholeWords"/>
                    <TextBlock 
                        Text="Learn about security settings like SmartScreen, remote access, and privacy options explained in plain language" 
                        FontSize="18" 
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Recommendation Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,32">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Location Card -->
                <Border 
                    Grid.Row="0" Grid.Column="0"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2"
                    CornerRadius="12"
                    Padding="28"
                    Margin="0,0,12,12">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <FontIcon 
                                Glyph="&#xe707;"
                                Style="{StaticResource SectionIconStyle}"
                                Foreground="#F14F21"/>
                            <TextBlock 
                                Text="Location" 
                                Style="{StaticResource SectionTitleStyle}"/>
                        </StackPanel>
                        <TextBlock
                            Style="{StaticResource SectionBodyText}"
                            Text="Super spies like to download trackers onto your computer. You think you downloaded a cute little clicker game? No, it's the KGB finding a way to spy on your every move! Catch them in the act and see what apps are being used to keep you under the threat of espionage."
                            />
                        <Button Content="View Location Settings" />
                    </StackPanel>
                </Border>

                <!-- Lock Time Card -->
                <Border 
                    Grid.Row="0" Grid.Column="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2"
                    CornerRadius="12"
                    Padding="28"
                    Margin="12,0,0,12">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <FontIcon 
                                Glyph="&#xf182;" 
                                Style="{StaticResource SectionIconStyle}" 
                                Foreground="#7EB900"/>
                            <TextBlock
                                Text="Screen Time Out"
                                Style="{StaticResource SectionTitleStyle}" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Network Card -->
                <Border 
                    Grid.Row="1" Grid.Column="0"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2"
                    CornerRadius="12"
                    Padding="28"
                    Margin="0,12,12,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <FontIcon 
                                Glyph="&#xe777;" 
                                Style="{StaticResource SectionIconStyle}" 
                                Foreground="#00A3EE"/>
                            <TextBlock
                                Text="Windows Update"
                                Style="{StaticResource SectionTitleStyle}" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Disk Card -->
                <Border 
                    Grid.Row="1" Grid.Column="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="2"
                    CornerRadius="12"
                    Padding="28"
                    Margin="12,12,0,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <FontIcon 
                                Glyph="&#xe72e;" 
                                Style="{StaticResource SectionIconStyle}" 
                                Foreground="#FEB800"/>
                            <TextBlock
                                Text="Sign-in Options"
                                Style="{StaticResource SectionTitleStyle}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Filter and Sort Controls -->
            <!--<Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,24,0">
                    <TextBlock 
                        Text="Show:" 
                        FontSize="16" 
                        VerticalAlignment="Center"
                        Margin="0,0,12,0"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <ComboBox 
                        ItemsSource="{Binding FilterOptions}"
                        SelectedItem="{Binding SelectedFilterOption, Mode=TwoWay}"
                        MinWidth="150"
                        FontSize="15"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock 
                        Text="Sort by:" 
                        FontSize="16" 
                        VerticalAlignment="Center"
                        Margin="0,0,12,0"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <ComboBox 
                        ItemsSource="{Binding SortOptions}"
                        SelectedItem="{Binding SelectedSortOption, Mode=TwoWay}"
                        MinWidth="150"
                        FontSize="15"/>
                </StackPanel>
            </Grid>

            -->
            <!-- Process Lists -->
            <!--
            <StackPanel Grid.Row="3" Spacing="16">

                -->
            <!-- Top Processes Section -->
            <!--
                <StackPanel>
                    <TextBlock 
                        Text="📊 Top Apps Using Resources" 
                        FontSize="24" 
                        FontWeight="SemiBold"
                        Margin="0,0,0,16"/>

                    <ItemsRepeater ItemsSource="{Binding TopProcesses, Mode=OneWay}">
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate>
                                <Border 
                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="20"
                                    Margin="0,0,0,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        -->
            <!-- Icon Placeholder -->
            <!--
                                        <Border 
                                            Grid.Column="0"
                                            Width="40"
                                            Height="40"
                                            Background="#2196F3"
                                            CornerRadius="8"
                                            Margin="0,0,20,0"
                                            VerticalAlignment="Center">
                                            <TextBlock 
                                                Text="📦"
                                                FontSize="24"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                                        </Border>

                                        -->
            <!-- App Info -->
            <!--
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock 
                                                Text="{Binding Description}" 
                                                FontSize="18" 
                                                FontWeight="SemiBold"/>
                                            <TextBlock 
                                                Text="{Binding FriendlyExplanation}" 
                                                FontSize="14"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                TextWrapping="Wrap"
                                                MaxWidth="500"
                                                Margin="0,4,0,0"/>
                                        </StackPanel>

                                        -->
            <!-- CPU Usage -->
            <!--
                                        <StackPanel Grid.Column="2" Margin="20,0" VerticalAlignment="Center" MinWidth="80">
                                            <TextBlock 
                                                Text="CPU" 
                                                FontSize="12"
                                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                HorizontalAlignment="Center"/>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                                                <TextBlock 
                                                    Text="{Binding CpuUsagePercent}" 
                                                    FontSize="18" 
                                                    FontWeight="SemiBold"
                                                    Foreground="#2196F3"/>
                                                <TextBlock 
                                                    Text="%" 
                                                    FontSize="18" 
                                                    FontWeight="SemiBold"
                                                    Foreground="#2196F3"
                                                    Margin="2,0,0,0"/>
                                            </StackPanel>
                                        </StackPanel>

                                        -->
            <!-- Memory Usage -->
            <!--
                                        <StackPanel Grid.Column="3" Margin="20,0" VerticalAlignment="Center" MinWidth="100">
                                            <TextBlock 
                                                Text="Memory" 
                                                FontSize="12"
                                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                HorizontalAlignment="Center"/>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                                                <TextBlock 
                                                    Text="{Binding MemoryUsageMB}" 
                                                    FontSize="18" 
                                                    FontWeight="SemiBold"
                                                    Foreground="#FF9800"/>
                                                <TextBlock 
                                                    Text=" MB" 
                                                    FontSize="18" 
                                                    FontWeight="SemiBold"
                                                    Foreground="#FF9800"
                                                    Margin="4,0,0,0"/>
                                            </StackPanel>
                                        </StackPanel>

                                        -->
            <!-- Action Buttons -->
            <!--
                                        <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="8" Margin="20,0,0,0" VerticalAlignment="Center">
                                            <Button 
                                                Content="🔄 Restart"
                                                Command="{Binding DataContext.RestartProcessCommand, ElementName=TaskManagerPageRoot}"
                                                CommandParameter="{Binding}"
                                                Foreground="#616161"
                                                Background="Transparent"
                                                BorderBrush="#616161"
                                                BorderThickness="1"
                                                Padding="12,8">
                                                <Button.Resources>
                                                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#616161"/>
                                                    <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="White"/>
                                                </Button.Resources>
                                            </Button>
                                            <Button 
                                                Content="🛑 End Task"
                                                Command="{Binding DataContext.EndProcessCommand, ElementName=TaskManagerPageRoot}"
                                                CommandParameter="{Binding}"
                                                Background="#F44336"
                                                Foreground="White"
                                                Padding="12,8"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </StackPanel>

            </StackPanel>-->

            <!-- Loading Overlay -->
            <!--<Border
                Grid.Row="0" 
                Grid.RowSpan="4"
                Background="#CC000000"
                Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Spacing="16">
                    <ProgressRing 
                        IsActive="True"
                        Width="80"
                        Height="80"/>
                    <TextBlock 
                        Text="Loading system information..."
                        FontSize="18"
                        FontWeight="SemiBold"
                        HorizontalAlignment="Center"
                        Foreground="White"/>
                </StackPanel>
            </Border>-->
        </Grid>
    </ScrollViewer>
</Page>